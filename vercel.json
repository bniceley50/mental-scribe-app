{
  "installCommand": "rm -rf node_modules package-lock.json && npm install --include=optional",
  "buildCommand": "npm run build",
  "build": {
    "env": {
      "NPM_CONFIG_IGNORE_OPTIONAL": "false"
    }
  }
}